$(function() {
  $("#posting_tags").tokenInput("/postings/tags.json", {
    crossDomain: false,
    prePopulate: $("#posting_tags").data("pre"),
    theme: "facebook",
    hintText: "search existing tag / bestehenden Tag suchen",
    preventDuplicates: true,
    tokenValue: 'name'
  });
});

$(function() {
  $("#user_notification_recipients").tokenInput("/user_notifications/emails.json", {
    crossDomain: false,
    theme: "facebook",
    hintText: "Blank => all users / leer => alle",
    preventDuplicates: true,
    tokenValue: 'name'
  });
});

$(function() {
  $("#user_group_member_tokens").tokenInput("/users/autocomplete_ids.json", {
    crossDomain: false,
    theme: "facebook",
    prePopulate: $("#user_group_member_tokens").data("pre"),
    hintText: "Search users",
    preventDuplicates: true,
    tokenValue: 'id'
  });
});