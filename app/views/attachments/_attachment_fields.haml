.attachment.fields
  - if f.object && f.object.file_file_name
    .attachment-label= raw(label_for_attachment(f))
  - else
    .attachment-label= t(:file)
  .file-field
    %span= f.file_field :file
  = link_to_remove_fields( t(:destroy_asset), f )
        

