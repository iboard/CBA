- title t(:blogs), false


%div
  -if @blogs.total_pages > 1
    .pagination_box= will_paginate @blogs

  - @blogs.each do |blog|
    = render :partial => 'blog_intro', :locals => { :blog => blog }
    %p.content_seperator
  
  -if @blogs.total_pages > 1
    .pagination_box= will_paginate @blogs