<% title t(:blogs), false %>
<% setup_action_buttons %>

<div>
  <%= will_paginate @blogs %>
  
  <% @blogs.each do |blog| %>
   <%= render :partial => 'blog_intro', :locals => { :blog => blog } %>
   <p class='content_seperator'></p>
  <% end %>
  
  <%= will_paginate @blogs %>
</div>
