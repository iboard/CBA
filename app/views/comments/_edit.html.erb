<%= form_for [@comment], :remote => true  do |f| %>

  <%= render :partial => 'comment_fields',
             :locals => {  :f => f,
                           :comment => @comment,
                           :commentable => @commentable
                        }
  %>
<% end %>
