<% if @page.page_template.nil? %>
  <%= render :partial => 'page', :locals => {:page => @page } %>
<% else %>
  <% title @page.t(I18n.locale, :title), false %>
  <%= raw @page.render_body(self) %>
<% end %>
