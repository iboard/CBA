- title t(:listing_page_templates)
#page_template_list
  - @pages.each do |page|
    .page_template
      %h1= page.title
      %address
        = link_to t(:edit), edit_page_path(page)
        |
        = link_to t(:delete), page, :method => :delete, :confirm => t(:are_you_sure)