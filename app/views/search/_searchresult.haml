#results
  - if search.any?
    - for hit in search
      .result
        %h3
          = link_to( hit[0].title + " (#{hit[0].class.to_s.humanize})", hit[0])
          = "[#{'*'*hit[1]}]"
        = ContentItem::markdown(hit[0].intro)
  - else
    =  t(:nothing_found)